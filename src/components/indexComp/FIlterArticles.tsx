import { Grid } from '@mui/material';

type Props = {};

const FIlterArticles = (props: Props) => {
  return (
    <Grid
      item
      container
      xs={12}
      spacing={5}
      textAlign={'center'}
      display={'flex'}
      sx={{ mt: '5%', mb: '5%' }}
    >
      <div>testing</div>
    </Grid>
  );
};

export default FIlterArticles;
